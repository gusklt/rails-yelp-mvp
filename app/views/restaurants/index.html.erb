<h1>Restaurants</h1>
<div class=restaurants>
  <ul>
    <% @restaurants.each do |restaurant| %>
    <li><%= link_to restaurant.name , restaurant_path(restaurant.id) %> | <%= restaurant.category %></li>
    <% end %>
  </ul>
</div>
<hr>
<%= link_to "Add a new restaurant", new_restaurant_path %>
